# command to install dependencies
machine:
  environment:
    COVERALLS_REPO_TOKEN: T02aqvKONBtjwPW4Q4EI15qOVHTBxMkoj

dependencies:
    pre:
        - make deps-dev-upgrade
        - pip install python-coveralls pytest-cov pytest-xdist

# command to run tests
test:
    override:
        - py.test -v --cov pyi_updater --cov-report term-missing
    post:
        - coveralls
