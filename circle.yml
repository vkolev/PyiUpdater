# command to install dependencies
dependencies:
    - "pip install -r requirements.txt"
    - "pip install coveralls pytest-cov pytest-xdist"

# command to run tests
test:
    override:
        py.test -v --cov src/pyi_updater -n 1

    post:
      coveralls