# command to install dependencies
dependencies:
    pre:
        - make deps-dev-upgrade
        - pip install coveralls pytest-cov pytest-xdist

# command to run tests
test:
    override:
        - coverage run setup.py ctest
    post:
        - coveralls
