language: python
python:
  - "2.7"

# command to install dependencies
install:
    - "pip install -r requirements.txt"
    - "pip install coveralls"
    - "pip install pytest"

# command to run tests
script:
    coverage run --source=src/pyi_updater setup.py test

after_success:
  coveralls
